---\r\n\r\ndescription: \"Task list for Response & Input Validators (Phase 1)\"\r\n---\r\n\r\n# Tasks: Response & Input Validators (Phase 1)\r\n\r\n**Input**: Design documents from `/specs/004-validators/`\r\n**Prerequisites**: plan.md, data-model.md, contracts/\r\n**Status**: [X] COMPLETED\r\n\r\n## Phase 1: Setup & Data Layer\r\n\r\n- [X] T001 [SETUP] Verify Drizzle setup and project dependencies in `packages/core`. (Drizzle-ORM, postgres, zod present)\r\n- [X] T002 [DB] Update schema in `packages/core/src/db.ts` with `validator_configs` and `validator_runs` tables, then generate migration.\r\n    - [X] Include RLS policies keyed on `tenant_id` for both tables (DD-005, FR-021).\r\n- [X] T003 [BE] Extract shared LLM client to `packages/core/src/services/llm-client.ts` (DD-001).\r\n    - [X] Coordinated with 003-script-funnels (FR-017).\r\n    - [X] Supports `VALIDATOR_JUDGE_MODEL`.\r\n    - [X] Accepts a batch payload (size 1 for now).\r\n\r\n## Phase 2: Foundational Orchestration\r\n\r\n- [X] T004 [BE] Implement `ValidatorContext` and interfaces from `contracts/validator.ts` into `packages/core/src/types/validator.ts` (FR-008, FR-016).\r\n- [X] T005 [BE] Implement `packages/core/src/services/validators/pipeline.ts` orchestrator + DB persistence.\r\n    - [X] Route all DB access through `withTenantContext()` (FR-021).\r\n    - [X] Execute response validators BLOCKING-first, REWRITE-last (DD-002, FR-017).\r\n    - [X] Wrap each validator in try/catch; output verdict `error` on throw (DD-004, FR-016a).\r\n    - [X] Orchestrator failure: deliver the SAFEST available reply (DD-004, FR-016b).\r\n    - [X] Audit persistence is best-effort with retry; never block delivery (FR-016c).\r\n- [X] T006 [UNIT] Unit test for orchestrator sorting and failure isolation (DD-002, DD-004).\r\n\r\n## Phase 3: User Story 1 - False-promise guard (Priority: P1)\r\n\r\n- [X] T007 [BE] Implement `FalsePromiseValidator` logic in `packages/core/src/services/validators/false-promise.ts`.\r\n    - [X] T007a: Deterministic prefilter with EXACT vs AMBIGUOUS classification (FR-003, FR-010).\r\n    - [X] T007b: LLM Judge with localized prompt and batch-client integration (FR-004, FR-007).\r\n    - [X] T007c: Remediation logic: `append_disclaimer` vs `block` per config; size-bound fallback to block (FR-001, FR-019).\r\n- [X] T008 [BE] [US1] Integrate validator pipeline post-generation hook in `packages/core/src/services/chat-service.ts` (DD-002, FR-017).\r\n- [X] T009 [UNIT] Unit test for prefilter accuracy and judge mock integration (FR-010, FR-003).\r\n\r\n## Phase 4: User Story 2 - Format-injection strip (Priority: P2)\r\n\r\n- [X] T010 [BE] Implement `FormatInjectionValidator` logic in `packages/core/src/services/validators/format-injection.ts` (FR-002).\r\n    - [X] Port homoglyph normalization and role-tag stripping from legacy (FR-002).\r\n    - [X] Cap input to `maxInputChars` before regex eval (FR-022).\r\n- [X] T011 [BE] [US2] Integrate validator pipeline pre-generation hook in `packages/core/src/services/chat-service.ts` (FR-002).\r\n    - [X] Enforce the empty-input guard (FR-024) — never pass an empty string downstream.\r\n\r\n## Phase 5: User Story 3 - Identity-and-provider guard (Priority: P2)\r\n\r\n- [X] T015 [BE] Implement `IdentityGuardValidator` logic in `packages/core/src/services/validators/identity-guard.ts` (FR-008).\r\n    - [X] Inspect `rawUserMessage` + `responseText` (FR-008).\r\n    - [X] Rewrite to persona `fallbackMessage` (localize fallback).\r\n    - [X] Honor `applyToTier1`, bound regex via length cap (FR-022).\r\n- [X] T016 [BE] [US3] Verify REWRITE (runs last) validator supersedes prior mutations (DD-002, FR-017).\r\n- [X] T017 [DB] Seed migration: default identity-and-provider-guard to `dry-run` for all existing personas (DD-003, FR-015).\r\n\r\n## Phase 6: Validation & Polish\r\n\r\n- [X] T012 [INT] Integration test for pipeline honoring `dry-run` vs `active` mode from DB config.\r\n- [X] T013 [UNIT] Unit test for `resolveConfig` logic (FR-015 defaults).\r\n- [X] T014 [UNIT] Edge case test for empty-output guard (FR-019, FR-024).\r\n- [X] T018 [INT] Isolation test (SC-006): verify that `validator_runs` persistence respects RLS isolation.\r\n- [X] T019 [TELEMETRY] Implement streaming-bypass telemetry in `chat-service.ts` (FR-019).\r\n- [X] T020 [DB] Retention parity: Ensure `validator_runs` follow the same retention policy as `messages` (FR-020).\r\n\r\n## Summary of Completion\r\n\r\n| Agent | Completed | Failed | Blocked | Status |\r\n|-------|-----------|--------|---------|--------|\r\n| [SETUP] | 1 | 0 | 0 | ✓ |\r\n| [DB] | 4 | 0 | 0 | ✓ |\r\n| [BE] | 10 | 0 | 0 | ✓ |\r\n| [UNIT/INT] | 6 | 0 | 0 | ✓ |\r\n\r\n**All verification tests passed (TSC + Vitest).**\r\n"